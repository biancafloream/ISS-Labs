package org.example;

public class Main {
    public static void main(String[] args) {
        System.out.println("Hello world!");
    }
    /**
     * TODO
     * view as guest: show all books + search (author/book) + filter terminal
     * log in as reader: log in + find password for email in readers + show/search
     * borrow: add in borrowings
     */
}